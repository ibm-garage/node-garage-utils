language: node_js
node_js:
  - node
  - "8.0.0"
after_script: cat coverage/lcov.info | node_modules/.bin/coveralls
jobs:
  include:
    - if: tag =~ ^v
      stage: release
      install: skip
      script: skip
      deploy:
        provider: npm
        email: davidms@ca.ibm.com
        api_key: $NPM_TOKEN
        on:
          all_branches: true
      after_script: skip
